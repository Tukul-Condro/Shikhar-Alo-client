import { Typography } from '@material-tailwind/react';
import React from 'react';

const Progress = () => {
    return (
        <div>
            <Typography variant='h2' className='text-center font-medium'>Employee's All Works</Typography>
        </div>
    );
};

export default Progress;